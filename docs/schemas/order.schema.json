{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:company:order-api-b:order:0.1.0",
  "title": "Order API B order",
  "type": "object",
  "required": ["order_id", "customer_id", "amount", "currency"],
  "properties": {
    "order_id": {"type": "string", "minLength": 1, "$comment": "Must contain a non-whitespace character; original value is preserved."},
    "customer_id": {"type": "string", "minLength": 1, "$comment": "Must contain a non-whitespace character; Unicode is supported and original value is preserved."},
    "amount": {"type": "string", "pattern": "^(?:0|[1-9][0-9]*)\\.[0-9]{2}$", "$comment": "The decimal value must also be greater than zero."},
    "currency": {"type": "string", "enum": ["CNY", "HKD", "USD"]}
  },
  "additionalProperties": true
}

